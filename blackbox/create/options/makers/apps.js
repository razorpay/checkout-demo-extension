function makeOptions(features, options) {
  return options;
}

function makePreferences(features, preferences) {
  // Modify preferences based on the features if you want.
  return preferences;
}

module.exports = {
  makeOptions,
  makePreferences,
};

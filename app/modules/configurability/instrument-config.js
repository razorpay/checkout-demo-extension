const config = {
  card: {
    keys: ['card_types', 'iins', 'issuers', 'networks'],
  },

  netbanking: {
    keys: ['banks'],
  },

  wallet: {
    keys: ['wallets'],
  },

  // TODO: Add more methods
};

export default config;

window.Razorpay = function(options){
  if(typeof this.configure == 'function')
    this.configure(options);
};
(function(){
  var exports = null;
  var module = { exports: null };

  // jquery
  <%- include jquery-1.11.1.js %>
  Razorpay.$ = module.exports;

  // hedwig
  <%- include hedwig.js %>
  Razorpay.Hedwig = module.exports;

  // DOM purify for user input sanitization
  <%- include purify.js %>
  Razorpay.DOMPurify = module.exports;

  // doT
  <%- include doT.js %>
  Razorpay.doT = module.exports;
  Razorpay.templates = {};

  // full screen mode for mobile
  Razorpay.curtainMode = false;
})();

.options
  border-radius: 2px;
  max-height: 320px;
  overflow: hidden;
  overflow-y: auto;
  background: #fff;

  &:not(.options-no-margin)
    margin-left: -12px;
    margin-right: -12px;

  .option
    border: 1px solid #E6E7E8;
    border-bottom: none;
    transition: 0.2s background;
    background: #fff;
    padding: 12px 10px;
    font-size: 14px;
    line-height: 20px;
    color: #333333;
    cursor: pointer;
    text-align: left;
    position: relative;
    transform: translate3d(0, 0, 0);

    .option-icon
      margin: auto;
      position: absolute;
      left: 16px;
      text-align: center;
      width: 22px;
      height: calc(100% - 12px);
      display: flex;
      align-items: flex-start;
      top: 0;
      margin-top: 12px;

      i
        font-size: 18px;

      svg
        width: 100%;
        height: 20px;
        max-width: 20px;
        display: inline-block;
        vertical-align: middle;

      img
        height: 22px;
        width: auto;

    &:first-child
      border-top-left-radius: 2px;
      border-top-right-radius: 2px;

    &:last-child
      border-bottom: 1px solid #E6E7E8;
      border-bottom-left-radius: 2px;
      border-bottom-right-radius: 2px;

    &.next-option
      padding-left: 46px;
      padding-right: 32px;

      &:after
        content: '\e604';
        font-size: 10px;
        position: absolute;
        right: 16px;
        top: 50%;
        transform: translateY(-50%) rotate(180deg);
        // margin-top: 0.2em;

      &.up-arrow::after
        transform: translateY(-50%) rotate(90deg);
        margin-top: 0;

      &.down-arrow::after
        transform: translateY(-50%) rotate(270deg);
        margin-top: 0;

      .option-title,
      .option-arrow-text
        display: inline-block;
        vertical-align: top;

      .option-arrow-text
        float: right;

    &.dark
      background-color: #F7F7F7;
      padding-left: 48px;

    &.radio-option, &.other-methods
      padding-left: 48px;
      padding-right: 12px;

      &.selected + .option
        border-top: 0;

      .option-title,
      .input-radio
        display: inline-block;
        vertical-align: top;

      .input-radio
        float: right;
        margin-top: -2px;

      .cvv-input
        position: absolute;
        right: 24px;
        top: 8px;
        font-size: 12px;
        -webkit-transition: 0.3s;
        -o-transition: 0.3s;
        transition: 0.3s;
        padding: 5px;
        width: 26px;
        border-bottom: 1px solid #ccc;

    &.radio-option.reverse
      padding-left: 16px;

      &.has-icon
        padding-right: 48px;

      .option-icon
        left: unset;
        right: 14px;

      .input-radio
        float: left;
        margin-top: -2px;
        margin-right: 4px;

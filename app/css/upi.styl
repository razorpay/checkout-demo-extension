@require 'common';

#form-upi {
  .input {
    padding: 12px 0 12px 50px;
    box-sizing: border-box;
    opacity: 1;
  }
  .elem-wrap {
    height: auto;
  }
  .elem {
    background: #fafafa url('https://cdn.razorpay.com/checkout/vpa.svg') no-repeat 15px center;
    border: 1px solid #eee;
    background-size: 24px;
  }
}

#cancel_upi {
  display: none;
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background: #fff;
  border-radius: inherit;

  p {
    margin: 20px;
    font-weight: bold;
    text-align: left;
  }

  label {
    text-align: left;
    padding: 7px 0 7px 50px;
    display: block;
    position: relative;
  }

  input[type=radio] {
    display: inline;
    position: absolute;
    left: 20px;
  }

  button {
    margin-top: 10px;
  }
}

#error-message.cancel_upi {
  height: 260px;
  overflow: hidden;

  #cancel_upi {
    display: block;
  }
}
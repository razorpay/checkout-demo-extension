button{
  background: {{=it}};
}
#tab-bar{
  background: {{=it}};
}
.radio-label:hover, input[type=radio]:checked + label{
  border-color: {{=it}};
}
#tab-wallet input:checked+label:after{
  color: {{=it}};
}